# Ultra Scraper – production pinned dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE for Render deployment:
#   You MUST add the following commands in Render's "Build Command" field
#   (or your Dockerfile) so Playwright's Chromium is available:
#
#       playwright install chromium
#       playwright install-deps chromium
#
# ──────────────────────────────────────────────────────────────────────────────

# --- HTTP / TLS fingerprinting ---
curl-cffi>=0.15.0

# --- HTML parsing + stealth browser fallback (includes Playwright) ---
scrapling[fetchers]==0.4.12

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation ---
pydantic==2.10.4

# --- Runtime ---
anyio==4.8.0
sniffio==1.3.1
