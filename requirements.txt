# Ultra Scraper – production pinned dependencies
#
# NOTE for Render deployment:
#   Render's native Python builder has issues with patchright.
#   Use a Dockerfile instead (see Dockerfile in this directory).
#   Steps:
#     1. Push Dockerfile to repo
#     2. Clear Build Command + Start Command in Render dashboard
#     3. Manual Deploy → "Clear build cache & deploy"
#
# ──────────────────────────────────────────────────────────────────────────────

# --- HTTP / TLS fingerprinting ---
curl-cffi>=0.15.0

# --- HTML parsing + stealth browser fallback (includes Playwright) ---
scrapling[fetchers]==0.4.12

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation ---
pydantic==2.10.4

# (runtime deps like anyio, sniffio, idna are resolved automatically)
