# Ultra Scraper – production pinned dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE for Render deployment:
#   Set the build command to:
#
#       pip install -r requirements.txt && patchright install chromium
#
#   (Do NOT include "patchright install-deps" — it requires root and will
#    fail on Render.  Render's base image already has the needed system
#    libraries for Chromium.)
#
# ──────────────────────────────────────────────────────────────────────────────

# --- HTTP / TLS fingerprinting ---
curl-cffi>=0.15.0

# --- HTML parsing + stealth browser fallback (includes Playwright) ---
scrapling[fetchers]==0.4.12

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation ---
pydantic==2.10.4

# (runtime deps like anyio, sniffio, idna are resolved automatically)
